#ifndef SHIP_SECONDARIES
#define SHIP_SECONDARIES

void healthBar();
char* concatHealth(int num);
void tortureHealth();
void bulletTime();
void playerShipMove();

#endif /* SHIP_SECONDARIES */
